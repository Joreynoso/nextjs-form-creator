name: Code Rabbit Review

on:
  pull_request:
    branches: [ "master" ]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: coderabbithq/code-rabbit-action@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
